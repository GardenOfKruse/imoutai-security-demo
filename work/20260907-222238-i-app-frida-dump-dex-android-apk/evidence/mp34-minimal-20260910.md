# mp34 最小足迹实验记录（2026-09-10）

> 本文保留首次端点不可达的历史结果。端点随后恢复，延迟附加与反射已成功，最新状态见 `mp34-reflection-results-20260910.md`。下述“未创建目标进程”仅适用于首次尝试。

## 目的

验证一个不安装异常处理器、保护观察器、线程入口替换、FindClass 修补、native 信号拦截、maps 过滤和实时盲扫的最小 Frida 观察模式。仅保留 nativeLoad 改写、MIUI ForceDark 观察、DEX 入口捕获、RegisterNatives 观察和类加载时间线。

## 实施变更

- `hooks/dump-dex-hook.js` 增加 `MP34_MINIMAL` 开关，默认 `false`，因此默认模式行为不变。
- `hooks/obs-driver.py` 增加 `--mp34`，通过脚本加载前的内存前缀启用该模式；不修改 APK、设备文件或网络配置。
- Python 源码语法检查通过。

## 执行

```text
python.exe obs-driver.py --js dump-dex-hook.js --out extract/obs-mp34-minimal-20260910 --wait 35 --mp34
```

设备 `82e459fc0920` 的 ADB 状态为 `device`。执行在 `dev.spawn(["com.moutai.mall"])` 处终止，错误为：

```text
frida.ServerNotRunningError: unable to connect to remote frida-server
```

本机只读检查显示 `127.0.0.1:8899` 无监听进程；`frida-ps -H 127.0.0.1:8899` 同样无法连接。

## 结果分类

- Evidence：mp34 脚本/驱动改动已完成；ADB 设备在线；Frida 远端服务不可用。
- Finding：本次未创建目标进程，也未加载 hook、未运行 Java/native 观察逻辑、未捕获 DEX、未得到 RegisterNatives 或类加载结果。
- UNVERIFIED：mp34 是否能使进程存活超过 30 秒；SIGSEGV 按需解密假设；任何加固检测或异常检测是否被绕过；登录签名是否恢复。
- 环境阻塞：Frida server 未运行/不可达。按案例纪律不重复连接重试，也不把本次结果解释为目标应用行为。
